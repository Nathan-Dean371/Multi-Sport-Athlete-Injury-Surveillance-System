# Admin Dashboard

Legacy module README restored during documentation migration.

For project-level canonical guidance, use:

- `docs/canonical/README.md`
- `docs/canonical/features/report-builder.md`
- `docs/canonical/getting-started/backend.md`

## Typical Local Run

```powershell
cd web/admin-dashboard
npm install
npm run dev
```

Ensure backend is running at `http://localhost:3000`.
