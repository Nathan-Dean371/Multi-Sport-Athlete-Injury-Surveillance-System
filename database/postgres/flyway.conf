# Flyway configuration (local/default)
# Override sensitive values via environment variables in CI/production.

flyway.url=jdbc:postgresql://127.0.0.1:5433/identity_service_test
flyway.user=identity_admin
flyway.password=identity-service-dev-password
flyway.locations=filesystem:database/postgres/migrations
flyway.connectRetries=60
