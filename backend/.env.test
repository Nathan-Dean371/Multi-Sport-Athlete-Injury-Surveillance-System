# Test Environment Configuration
# Used for integration tests (E2E tests)

# Application
PORT=3001
NODE_ENV=test

# Neo4j Database (separate test container - safe to clean!)
NEO4J_URI=bolt://localhost:7688
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=injury-surveillance-test-password
NEO4J_DATABASE=neo4j

# PostgreSQL Database (separate test database)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=identity_service_test
POSTGRES_USER=identity_admin
POSTGRES_PASSWORD=identity-service-dev-password

# JWT Settings (same as dev, but could be different for testing)
JWT_SECRET=test-secret-key-for-integration-tests
JWT_EXPIRES_IN=1h
