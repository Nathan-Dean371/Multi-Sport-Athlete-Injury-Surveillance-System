# Mobile App

Legacy module README restored during documentation migration.

## Canonical Documentation

Use the canonical docs tree for current setup and run instructions:

- `docs/canonical/getting-started/mobile.md`
- `docs/canonical/getting-started/demo.md`

## Local Commands

```powershell
cd mobile
npm install
npm start
```

For root-level scripted startup:

```powershell
npm run mobile:dev
npm run mobile:prod
```
